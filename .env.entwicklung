# Nur fuer "npm run server" auf dem eigenen Rechner. Auf Azure kommen die
# Werte aus den Anwendungseinstellungen, dort gibt es diese Datei nicht.
PORT=8080
DATEN_DATEI=.daten/arbeitskleidung.json
DEV_EMAIL=feyen@isotec-morscheck.de
DEV_NAME=Entwicklung
